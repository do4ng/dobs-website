```bash
$ dobs -h

Usage
  $ dobs [command] [options]

Commands
  dev
  build

Options (global)
  --help, -h           Show help
  --version, -v        Show version

Run '<command> --help' for more information on a command.
```
